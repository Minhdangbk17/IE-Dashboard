"""Namespace package chứa các Sub-Engine của Domain "dyeing" (oee, downtime, excel_import, ...).

Rỗng có chủ đích — `modules/dyeing/__init__.py` dùng `pkgutil` để quét
`engines.__path__` mà không cần import trực tiếp từng engine ở đây.
"""
